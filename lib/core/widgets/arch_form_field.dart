import 'package:flutter/material.dart';

enum Architecture {
  amd64,
  arm64,
  armv7,
  ppc64le,
}

class ArchFormField extends StatelessWidget {
  const ArchFormField({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }
}
